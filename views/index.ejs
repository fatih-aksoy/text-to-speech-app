<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Text to Speech Converter</title>
    <style>
      /* Default styles for the form */
      form {
        max-width: 600px; /* Set maximum width for the form */
        margin: 0 auto; /* Center the form horizontally */
        padding: 0 20px; /* Add padding to the sides */
        /* margin-right: 30px; */
      }

      textarea {
        width: 100%;
        height: 250px;
        background: #403d84;
        color: #fff;
        font-size: 15px;
        border: 0;
        outline: 0;
        padding: 20px;
        border-radius: 10px;
        resize: none;
        margin-bottom: 30px;
      }

      select,
      button {
        width: 30%;
        padding: 10px;
        border-radius: 35px;
        border: 1;
        outline: 0;
        cursor: pointer;
        margin-bottom: 20px;
      }

      button {
        background-color: #ff2963;
        color: #fff;
        font-size: 16px;
      }

      h1 {
        font-size: 45px;
        font-weight: 500;
        margin-top: 50px;
        margin-bottom: 50px;
      }
      div {
        width: 100%;
        min-height: 100vh;
        background: linear-gradient(90deg, #035cb9, #62b0a6);
        display: flex;
        align-items: center;
        justify-content: center;
        flex-direction: column;
        color: #fff;
      }

      /* Media query for mobile devices */
      @media screen and (max-width: 600px) {
        textarea {
          height: 150px; /* Reduce height of textarea for mobile */
          font-size: 12px; /* Reduce font size for mobile */
        }
      }
    </style>
  </head>
  <body>
    <div>
      <h1>Text to Speech Converter</h1>
      <form action="/convert" method="post">
        <textarea name="text" rows="4" cols="50"></textarea><br />
        <label for="language">Select Language:</label>
        <select name="language" id="language">
          <option value="en">English</option>
          <option value="tr">Turkish</option>
          <option value="sv">Swedish</option>
          <!-- Add more language options as needed --></select
        ><br />
        <button type="submit">Convert to Speech</button>
      </form>
    </div>
  </body>
</html>
